# ------------------------------------------------------------------------------
# Frontend Environment Configuration (Vite)
# ------------------------------------------------------------------------------

# The absolute URL of your cloud-hosted FastAPI backend (e.g. Render).
# Defaults to '/api' for local Vite proxy if omitted.
VITE_API_URL=https://cloud-finops-intel.onrender.com/api
